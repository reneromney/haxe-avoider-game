package ;

import openfl.Assets;

/**
 *
 * GameSettings.hx: A set of global variables used for declaring game options. 
 */
class GameSettings
{
	//Variables for determining whether or not a Restart or Start Menu kicks in
	public static var mainTitle : Bool = true;
	public static var restart   : Bool = false;	
	

}